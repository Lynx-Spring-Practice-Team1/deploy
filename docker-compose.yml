services:

  # ---------------------------------------------------------------------------
  # INFRASTRUCTURE
  # ---------------------------------------------------------------------------

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init:/docker-entrypoint-initdb.d
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - broker-network

  postgres-init:
    image: postgres:16-alpine
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGUSER: ${POSTGRES_USER}
      PGPASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    entrypoint:
      - /bin/sh
      - -ec
      - |
        set -eu
        for db in orders_db wallet_db portfolio_db market_notifications_db price_history_db; do
          if ! psql -h postgres -d "$${POSTGRES_DB}" -tAc "SELECT 1 FROM pg_database WHERE datname='$${db}'" | grep -q 1; then
            psql -h postgres -d "$${POSTGRES_DB}" -c "CREATE DATABASE \"$${db}\""
          fi
        done
    networks:
      - broker-network
    restart: "no"

  redpanda:
    image: redpandadata/redpanda:latest
    command:
      - redpanda
      - start
      - --mode=dev-container
      - --overprovisioned
      - --smp=1
      - --memory=512M
      - --reserve-memory=0M
      - --node-id=0
      - --check=false
      - --kafka-addr=internal://0.0.0.0:9092,external://0.0.0.0:19092
      - --advertise-kafka-addr=internal://redpanda:9092,external://localhost:${REDPANDA_KAFKA_EXTERNAL_PORT:-19093}
    ports:
      - "${REDPANDA_KAFKA_PORT:-9093}:9092"
      - "${REDPANDA_KAFKA_EXTERNAL_PORT:-19093}:19092"
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -E 'Healthy:.+true' || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
    networks:
      - broker-network

  redpanda-init:
    image: redpandadata/redpanda:latest
    depends_on:
      redpanda:
        condition: service_healthy
    entrypoint:
      - /bin/bash
      - -ec
      - |
        rpk --brokers redpanda:9092 topic create \
          order.created order.accepted order.rejected order.cancelled order.filled \
          market.price.updated portfolio.updated notification.created \
          --if-not-exists
    networks:
      - broker-network
    restart: "no"

  redpanda-console:
    image: redpandadata/console:latest
    environment:
      KAFKA_BROKERS: redpanda:9092
    ports:
      - "${REDPANDA_CONSOLE_PORT:-8090}:8080"
    depends_on:
      redpanda:
        condition: service_healthy
    networks:
      - broker-network

  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6380}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - broker-network

  # ---------------------------------------------------------------------------
  # APPLICATION SERVICES
  # Parent layout expected:
  #   parent/
  #     deploy/
  #     API-gateway/
  #     auth-service/
  #     order-service/
  #     wallet-service/
  #     portfolio-service/
  #     market-notifications/
  #     price-history-service/
  #     frontend/
  # ---------------------------------------------------------------------------

  api-gateway:
    image: ghcr.io/lynx-spring/practice-team1/api-gateway:latest
    ports:
      - "${API_GATEWAY_PORT:-8000}:8080"
    environment:
      JWT_SECRET: ${JWT_SECRET}
      AUTH_SERVICE_URL: http://auth-service:8000
      ORDER_SERVICE_URL: http://order-service:8002
      WALLET_SERVICE_URL: http://wallet-service:8003
      PORTFOLIO_SERVICE_URL: http://portfolio-service:8004
      MARKET_NOTIFICATIONS_URL: http://market-notifications:8005
      PRICE_HISTORY_SERVICE_URL: http://price-history-service:3001
    depends_on:
      - auth-service
      - order-service
      - wallet-service
      - portfolio-service
      - market-notifications
      - price-history-service
      - redis
    networks:
      - broker-network
    restart: unless-stopped

  auth-service:
    image: ghcr.io/lynx-spring/practice-team1/auth-service:latest
    ports:
      - "${AUTH_SERVICE_PORT:-8001}:8000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRY_MINUTES: ${JWT_EXPIRY_MINUTES:-60}
    depends_on:
      postgres:
        condition: service_healthy
      postgres-init:
        condition: service_completed_successfully
    networks:
      - broker-network
    restart: unless-stopped

  order-service:
    image: ghcr.io/lynx-spring/practice-team1/order-service:latest
    ports:
      - "${ORDER_SERVICE_PORT:-8002}:8002"
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/orders_db
      KAFKA_BOOTSTRAP_SERVERS: redpanda:9092
      EXCHANGE_API_BASE_URL: ${EXCHANGE_API_BASE_URL}
      EXCHANGE_WS_URL: ${EXCHANGE_WS_URL}
      EXCHANGE_API_KEY: ${EXCHANGE_API_KEY}
      EXCHANGE_API_SECRET: ${EXCHANGE_API_SECRET}
      JWT_SECRET: ${JWT_SECRET}
      WALLET_SERVICE_URL: http://wallet-service:8003
    depends_on:
      postgres:
        condition: service_healthy
      postgres-init:
        condition: service_completed_successfully
      redpanda:
        condition: service_healthy
      redpanda-init:
        condition: service_completed_successfully
      wallet-service:
        condition: service_started
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - broker-network
    restart: unless-stopped

  wallet-service:
    image: ghcr.io/lynx-spring/practice-team1/wallet-service:latest
    ports:
      - "${WALLET_SERVICE_PORT:-8003}:8003"
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/wallet_db
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      postgres:
        condition: service_healthy
      postgres-init:
        condition: service_completed_successfully
    networks:
      - broker-network
    restart: unless-stopped

  portfolio-service:
    image: ghcr.io/lynx-spring/practice-team1/portfolio-service:latest
    ports:
      - "${PORTFOLIO_SERVICE_PORT:-8004}:8004"
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/portfolio_db
      KAFKA_ENABLED: "true"
      KAFKA_BOOTSTRAP_SERVERS: redpanda:9092
      WALLET_SERVICE_URL: http://wallet-service:8003
      MARKET_WS_ENABLED: "true"
      MARKET_WS_URL: ${EXCHANGE_WS_URL}
      MARKET_WS_API_KEY: ${EXCHANGE_API_KEY}
      MARKET_WS_API_SECRET: ${EXCHANGE_API_SECRET}
    depends_on:
      postgres:
        condition: service_healthy
      postgres-init:
        condition: service_completed_successfully
      redpanda:
        condition: service_healthy
      redpanda-init:
        condition: service_completed_successfully
      wallet-service:
        condition: service_started
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - broker-network
    restart: unless-stopped

  market-notifications:
    image: ghcr.io/lynx-spring/practice-team1/market-notifications:latest
    ports:
      - "${MARKET_NOTIFICATIONS_PORT:-8005}:8005"
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/market_notifications_db
      KAFKA_ENABLED: "true"
      KAFKA_BOOTSTRAP_SERVERS: redpanda:9092
      EXCHANGE_WS_URL: ${EXCHANGE_WS_URL}
      EXCHANGE_WS_API_KEY: ${EXCHANGE_API_KEY}
      EXCHANGE_WS_API_SECRET: ${EXCHANGE_API_SECRET}
      MARKET_WS_ENABLED: "true"
    depends_on:
      postgres:
        condition: service_healthy
      postgres-init:
        condition: service_completed_successfully
      redpanda:
        condition: service_healthy
      redpanda-init:
        condition: service_completed_successfully
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - broker-network
    restart: unless-stopped

  price-history-service:
    image: ghcr.io/lynx-spring/practice-team1/price-history-service:latest
    ports:
      - "${PRICE_HISTORY_SERVICE_PORT:-3001}:3001"
    environment:
      PORT: 3001
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/price_history_db
    depends_on:
      postgres:
        condition: service_healthy
      postgres-init:
        condition: service_completed_successfully
    networks:
      - broker-network
    restart: unless-stopped

  frontend:
    image: ghcr.io/lynx-spring/practice-team1/frontend:latest
    ports:
      - "${FRONTEND_PORT:-3000}:5173"
    environment:
      API_TARGET: http://api-gateway:8080
      VITE_EXCHANGE_WS_URL: ${VITE_EXCHANGE_WS_URL}
      VITE_WS_KEY: ${EXCHANGE_API_KEY}
      VITE_WS_SECRET: ${EXCHANGE_API_SECRET}
    depends_on:
      - api-gateway
    networks:
      - broker-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  broker-network:
    driver: bridge
